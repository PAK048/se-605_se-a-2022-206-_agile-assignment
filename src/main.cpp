#include <iostream>
using namespace std;

// Student's function declaration (example)
int add(int a, int b) {
    // Student will implement this
    return a + b;
}

int main() {
    // Input for testing by Judge0
    int a, b;
    cin >> a >> b;

    // Output the result
    cout << add(a, b) << endl;

    return 0;
}
